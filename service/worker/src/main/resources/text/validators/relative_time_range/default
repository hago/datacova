imports "boundary"

Values of fields ["<fields; separator="\", \"">"] should be a time,
<if (lowerBound)>
later than <if (lowerBound.inclusive)>or equals <endif> the time that is
<if (isLowerYear)><lowerBound.timeDiff.year> year<if (isLowerYearPlural)>s<endif><endif>
<if (isLowerMonth)><lowerBound.timeDiff.month> month<if (isLowerMonthPlural)>s<endif><endif>
<if (isLowerDay)><lowerBound.timeDiff.day> day<if (isLowerDayPlural)>s<endif><endif>
<if (isLowerHour)><lowerBound.timeDiff.hour> hour<if (isLowerHourPlural)>s<endif><endif>
<if (isLowerBefore)>earlier<else>later<endif> of the moment of
<if (isLowerNow)>the execution
<elseif (isLowerBeginOfToday)>the start of day when executing
<elseif (isLowerEndOfToday)>the end of day when executing
<elseif (isLowerBeginOfThisMonth)>the start of month when executing
<elseif (isLowerEndOfThisMonth)>the end of month when executing
<elseif (isLowerBeginOfThisWeek)>the start of week when executing
<elseif (isLowerEndOfThisWeek)>the end of week when executing
<elseif (isLowerBeginOfThisQuarter)>the start of quarter when executing
<elseif (isLowerEndOfThisQuarter)>the end of quarter when executing
<elseif (isLowerBeginOfThisYear)>the start of year when executing
<elseif (isLowerEndOfThisYear)>the end of year when executing
<endif>, which is "<lowerTime>" for this execution.
<endif>
<if (upperBound)>
<if (lowerBound)> and <endif>
earlier than <if (upperBound.inclusive)>or equals <endif> the time that is
<if (isUpperYear)><upperBound.timeDiff.year> year<if (isUpperYearPlural)>s<endif><endif>
<if (isUpperMonth)><upperBound.timeDiff.month> month<if (isUpperMonthPlural)>s<endif><endif>
<if (isUpperDay)><upperBound.timeDiff.day> day<if (isUpperDayPlural)>s<endif><endif>
<if (isUpperHour)><upperBound.timeDiff.hour> hour<if (isUpperHourPlural)>s<endif><endif>
<if (isUpperBefore)>earlier<else>later<endif> of the moment of
<if (isUpperNow)>the execution
<elseif (isUpperBeginOfToday)>the start of day when executing
<elseif (isUpperEndOfToday)>the end of day when executing
<elseif (isUpperBeginOfThisMonth)>the start of month when executing
<elseif (isUpperEndOfThisMonth)>the end of month when executing
<elseif (isUpperBeginOfThisWeek)>the start of week when executing
<elseif (isUpperEndOfThisWeek)>the end of week when executing
<elseif (isUpperBeginOfThisQuarter)>the start of quarter when executing
<elseif (isUpperEndOfThisQuarter)>the end of quarter when executing
<elseif (isUpperBeginOfThisYear)>the start of year when executing
<elseif (isUpperEndOfThisYear)>the end of year when executing
<endif>, which is "<upperTime>" for this execution.
<endif>